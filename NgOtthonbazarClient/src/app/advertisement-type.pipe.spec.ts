import { AdvertisementTypePipe } from './advertisement-type.pipe';

describe('AdvertisementTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AdvertisementTypePipe();
    expect(pipe).toBeTruthy();
  });
});
